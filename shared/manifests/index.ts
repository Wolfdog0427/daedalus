/**
 * Daedalus shared manifests — placeholder for node and kernel
 * capability manifests.
 *
 * Intent: each node or kernel instance will publish a manifest
 * describing its identity, capabilities, and version. The
 * canonical manifest shape will be defined here.
 */
