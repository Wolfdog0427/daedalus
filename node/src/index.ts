/**
 * Daedalus Node runtime entrypoint.
 *
 * The node is a lightweight agent that connects to the orchestrator,
 * registers its capabilities, sends heartbeats, and executes
 * delegated tasks (shards, queries, tool calls).
 *
 * This is a v0.3 scaffold — full activation planned for v0.4.
 */

console.log('[daedalus-node] scaffold loaded — not yet active');
