/**
 * Daedalus Kernel entrypoint.
 *
 * The kernel is the future "thinking" layer — it hosts beings,
 * planners, memory systems, and the prime directive runtime.
 * It receives delegated work from the orchestrator and returns
 * structured results.
 *
 * This is a v0.3 scaffold — full activation planned for v0.4+.
 */

console.log('[daedalus-kernel] scaffold loaded — not yet active');
